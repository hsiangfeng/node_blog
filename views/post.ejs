<% extend('./layout/layout')%>
  <div class="container">
    
<div class="hex-blog">
  <nav class="breadcrumb pl-0">
    <a class="breadcrumb-item text-primary" href="#">Home</a>
    <a class="breadcrumb-item text-primary" href="#">
      <% for (let i in categories) { %>
        <% if (categories[i].id === article.category) {%>
          <%- categories[i].name %>
        <% } %>
      <% } %>
    </a>
    <span class="breadcrumb-item active"><%= article.title || '' %></span>
  </nav>
  <div class="row justify-content-center">
    <div class="col-md-9">
      <!-- artical header -->
      <h1 class="text-primary"><%= article.title || '' %></h1>
      <div class="d-flex flex-row text-secondary small">
        <div class="p-2"><i class="fa fa-user-o" aria-hidden="true"></i> 卡斯伯</div>
        <div class="p-2"><%- momont(article.update_time * 1000).format('YYYY/MM/DD') %></div>
        <div class="p-2"><i class="fa fa-tag" aria-hidden="true"></i> 
          <a href="" class="text-info">
            <% for (let i in categories) { %>
              <% if (categories[i].id === article.category) {%>
                <%- categories[i].name %>
              <% } %>
            <% } %>
          </a>
        </div>
      </div>
      <article class="text-dark post mb-4">
        <%- article.content || '' %>

      </article>
      <!-- 留言 -->
      <div class="card mb-3">
        <div class="card-body">
          
        </div>
      </div>

    </div>
  </div>
</div>

  </div>